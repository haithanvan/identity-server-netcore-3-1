version: '3.4'

services:    
  postgresql-data:
    networks:
      - default

  identity-api:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "5001:80"
    networks:
      - default
