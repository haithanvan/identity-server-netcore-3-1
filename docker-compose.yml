version: '3.4'

services:
  identity-postgresql-data:
    image: postgres:10
    environment:
      POSTGRES_PASSWORD: DigiMed123
    ports:
      - 5442:5432
    volumes:
      - identity-postgresql-data:/var/lib/postgresql/data

  identity-api:
    image: haitv/identity-api
    build:
      context: .
      dockerfile: IdentityServerDemo/Dockerfile

volumes:
  identity-postgresql-data:
    external: true

